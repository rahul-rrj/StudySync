# 🖼️ Image Editor - Flask App  

This is a simple web-based Image Editor built using **Flask**. It allows users to upload images, preview them, resize them, and download the processed image.  

---

## 📦 Requirements  

Before running the project, install the required Python libraries:  

```bash
pip install flask flask-cors pillow
